# Architecture

This document has been merged into:

- **[QUICKSTART_CUSTOM_STUDY.md](QUICKSTART_CUSTOM_STUDY.md)** — what MOF does, three layers, step-by-step usage
- **[DATA_CONTRACT.md](DATA_CONTRACT.md)** — schemas, configuration reference, objective extension levels

The architecture figure is at `mof_architecture_diagram.png` in the repository root
(regenerate with `python scripts/make_figure.py`).
