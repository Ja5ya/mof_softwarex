# Example environment for MIMIC pipeline runs (Layers 2–5).
# Copy to configs/env.mimic and edit paths.

export MOF_CONFIG=configs/mimic.yaml
export MOF_DATA_ROOT=/path/to/writable/mof_runs/mimic
export MOF_PREBUILT_ROOT=/path/to/prebuilt_mimic_run
